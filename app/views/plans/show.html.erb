<div class="content">
  <h1><%= @plan.title %></h1>
  <p><%= @plan.description %></p>

  <% for event in @plan.events -%>
    <div>
      <%= event.name %>
    </div>
  <% end -%>

  <% if @plan.is_editable_by?(current_user) %>
    <%= link_to 'Edit', edit_plan_path(@plan) %>
  <% end %>
</div>

