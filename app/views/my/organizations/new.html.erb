<%
  #accept = raw(ImageUploader.accepts.join(', '))
%>
<% context 'Create A New Organization' %>

<%= semantic_form_for @organization, :url => my_organizations_path(), :method => 'POST' do |form| %>
  <%= render :partial => 'fields', :locals => { :form => form } %>
<% end %>

