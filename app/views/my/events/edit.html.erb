<div class='form'>
  <h1>Editing <%= @event.name %></h1>

  <hr />

  <%= semantic_form_for @event, :url => my_events_path(@event), :method => 'PUT', :html => {:multipart => true} do |form| %>
    <%= render :partial => 'fields', :locals => { :form => form } %>
  <% end %>
</div>

