<li>
  <div class='image'>
    <%= link_to event_path(event.slug, event.id) do %>
      <%= image_tag(event.image.url(:medium)) %>
      <div class='description'><%= event.description %></div>
    <% end %>

    <%
      if logged_in?
        trend_state = current_user.events.include?(event) ? :trended : :trend
      else
        trend_state = :trend
      end
    -%>
    <%= link_to ' ', "#trend_event/#{event.id}", :class => ['trend-button', trend_state] %>
  </div>

  <%= link_to event_path(event.slug, event.id) do %>
    <div class='detail'>
      <h1 class='truncate'><%= event.name %></h1>

      <% unless @organization %>
        <div class="organization-name truncate">
          @ <%= event.location.name %>
        </div>
      <% end %>

      <%= render :partial => '/events/time_span', :locals => {:event => event} %>
    </div>
  <% end %>
</li>


